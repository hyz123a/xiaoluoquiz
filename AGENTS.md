# xiaoluoquiz Development Guide

## 项目目标

xiaoluoquiz 是一个面向约 200 名固定用户的 Rust 全栈刷题网站。管理员负责题目、答案、解析和简答题批改；普通用户负责练习和查看自己的答题记录。

产品需求和验收标准以 [`docs/user-story.md`](docs/user-story.md) 为准。实现过程中如果代码与文档冲突，应先更新用户故事或明确记录决策，再修改代码。

## 技术栈与边界

- 后端使用 Rust、Tokio、Axum。
- 数据库访问统一使用 **SQLx** 和 PostgreSQL；用户原文中的 `sqxl` 按 `SQLx` 处理。
- 前端使用 Yew 编译为 WASM；需要时使用 Trunk 或等价工具完成开发和发布构建。
- 样式使用 Tailwind CSS 和 daisyUI。
- Playwright 使用 TypeScript 编写端到端测试和测试辅助脚本。
- Node.js/TypeScript 只承担前端构建、样式处理和浏览器测试，不把核心业务迁移到另一套后端运行时。
- 后续使用 Docker 部署。应用容器、PostgreSQL 和反向代理的职责要保持清晰；不要让应用容器依赖宿主机上的隐式状态。

当前采用**轻量六边形架构的模块化单体**（Hexagonal Architecture）。六边形架构首先约束依赖方向和职责边界，不要求一次性把现有代码重写成完整的目录结构；只有当模块职责交叉或维护成本明显上升时，才进行渐进式拆分。

当前模块映射如下：

```text
src/domain/                 题目、答案、评分、试卷和认证等领域模型与不变量
src/application/            用例、应用服务以及面向外部能力的端口（trait）
src/server/http.rs          当前的 Axum HTTP 入站适配器
src/server/*_store.rs       当前的 SQLx/PostgreSQL 出站适配器
src/server/config.rs        当前的配置加载实现
src/web/                    Yew/WASM 前端
migrations/                 SQLx 数据库迁移
scripts/                    本地 PostgreSQL 和开发辅助脚本
tests/                      Rust 集成测试和 Playwright 端到端测试
```

随着复杂度增加，可以将适配器和配置逐步迁移到以下目录；迁移时应保持外部行为不变，避免为了目录名称进行大规模重写：

```text
src/adapters/inbound/       Axum HTTP 路由、提取器和响应转换
src/adapters/outbound/      SQLx 数据库、时钟等外部适配器
src/configuration/          配置加载和环境变量覆盖
```

依赖方向保持为：

```text
inbound/outbound adapters -> application -> domain
web                       -> HTTP API/共享契约，不得直接访问数据库
```

新增业务逻辑应放在领域层或用例层，不应放入 HTTP 处理器或 SQL 查询中。应用层通过端口访问可替换的存储等外部能力；确需抽象密码哈希、时钟或随机数时再增加对应端口，不为尚未存在的替换需求预先建立大量抽象。领域层不能依赖 Axum、SQLx、浏览器 API 或具体存储实现。路由层只负责 HTTP 适配和输入转换，用例层负责业务流程，数据库适配器负责持久化和事务边界。

## 题目与答案

支持五种题型：单选题、多选题、填空题、判断题、简答题。服务端负责题型校验和评分，浏览器提交的分数永远不可信。

- 单选题和多选题的正确答案使用稳定的选项标识，不使用页面数组下标；多选题支持可变数量的选项，并要求提交完整的正确选项集合。
- 填空题允许多个可接受答案，答案规范化规则要显式定义，不能在代码中隐式改变用户输入语义。
- 判断题使用布尔语义存储标准答案，不用中文显示文本参与评分。
- 简答题 MVP 采用待批改或用户自查，不把文本相似度自动当作最终成绩。
- 题目至少区分 `draft`、`published`、`archived` 三种状态。
- 题目发布前必须经过服务端完整性校验；普通用户只能读取已发布题目的题面。
- 提交前的用户接口不得返回正确答案、参考答案或评分规则中不应泄露的字段。
- 修改已被作答的题目时，必须保留历史题面和评分规则；优先使用版本或复制新题，不能破坏既有答题记录。
- 不直接渲染未经清洗的任意 HTML。解析首期使用纯文本或明确限制的 Markdown 子集。

## 配置与数据库

配置必须按“基础配置、环境配置、环境变量”分层加载：

```text
configuration/base.yaml        所有环境的非敏感默认值
configuration/local.yaml       本地开发覆盖值
configuration/production.yaml  云端/生产覆盖值
```

建议的配置优先级为：基础文件、环境专用文件、环境变量；敏感值以环境变量或部署平台 Secret 为最终来源。生产密钥、数据库 URL 和第三方服务令牌不能写入 YAML 或 Git。

本地数据库脚本必须负责数据库容器准备、健康检查、应用用户和数据库创建，以及 migration 执行；不要把这些初始化动作复制到应用启动流程：

1. 检查 Docker、PostgreSQL 和 `sqlx-cli` 是否可用。
2. 启动临时或可复用的本地 PostgreSQL 实例，并等待健康检查通过。
3. 创建应用数据库和最小权限应用用户。
4. 导出 `DATABASE_URL`，运行 `sqlx migrate run`。
5. 失败时返回非零退出码，并明确显示下一步操作。

数据库约定：

- 每次结构变更先新增迁移文件，不修改已经应用的迁移。
- 迁移文件按 SQLx 规则命名，并在干净数据库和已有数据库上分别验证。
- 迁移默认向前演进；确需回滚时写清楚对应策略，不依赖手工删除表。
- 生产部署把迁移作为明确的发布步骤或一次性任务执行，不在多个应用副本中竞争执行不受控的初始化逻辑。
- SQL 查询优先使用 SQLx 的类型检查能力；查询、数据库约束和领域校验不能只依赖其中一层。
- 考试开放、截止、开始和交卷时间统一按 `Asia/Shanghai`（UTC+08:00）解释和展示；数据库继续使用 `timestamptz` 保存绝对时间。
- PostgreSQL 连接池大小、超时和 SSL 要求放在环境配置中，不能硬编码。
- Docker 部署和本地数据库脚本不设置 `HTTP_PROXY`、`HTTPS_PROXY`，也不依赖 registry mirror；镜像应直接从配置的 Docker registry 获取。若开发机已有系统级 Docker 代理，应先清理或明确记录，不能悄悄写进项目脚本。

## 强制开发流程：Red-Green-Refactor

测试即文档。所有行为或代码变更必须按以下顺序循环，不能只做到测试变绿：

1. **Red**：先写一个只描述单一外部行为的失败测试，运行最小范围测试，确认失败原因正是缺少该行为。
2. **Green**：用最小实现让测试通过，不夹带无关功能或重构。
3. **Refactor**：测试变绿后整理代码和测试，消除重复、收敛抽象、修正依赖方向或改善命名；每次整理后重新运行测试。

完成 Refactor 后，再从 Red 开始补充边界、错误、回滚和并发场景。没有完成 Refactor，不能把任务标记为完成。

纯文档修改不需要为了形式伪造业务测试，但必须检查 Markdown 结构、链接、文件路径和内容之间的一致性。

## 测试要求

### Rust 测试

- 领域模型、答案校验和评分规则优先写快速单元测试。
- Axum 路由、业务用例和数据库约束使用集成测试覆盖。
- 涉及 PostgreSQL 语义的测试使用真实 PostgreSQL，不用内存数据库替代。
- 测试题目发布、答案泄露、重复提交、答题记录隔离和题目版本变化。
- 需要数据库时先运行本地初始化脚本，并确保测试数据不会依赖开发机已有数据。

### Playwright 端到端测试

端到端测试必须按真实用户流程操作，而不是只检查页面能渲染：

- 管理员创建、预览、发布和下线五种题型。
- 普通用户练习、提交、查看结果和查看答题记录。
- 简答题提交、管理员批改、用户查看批改结果。
- 空列表、加载失败、表单校验失败、重复提交和移动端布局。

涉及 UI、路由、客户端状态或页面数据的改动，必须在浏览器中完成端到端验证；只看一张截图不算验证。至少检查桌面和移动视口，以及所有共享相同状态或组件的页面。

## UI 约定

- Yew 组件负责交互和状态展示，业务规则仍由后端负责。
- 使用 Tailwind 工具类和 daisyUI 组件保持视觉一致，不在组件中重复创建同一套基础控件样式。
- 所有表单控件有可关联的标签、清晰的错误提示和键盘可操作状态。
- 每个异步页面都定义加载中、成功、空数据和失败状态。
- 题目答案、解析和用户输入按信任边界处理，禁止不必要的 HTML 注入。
- 桌面布局和移动布局都必须可用；长题干、长选项、长简答答案不能造成横向溢出。

## 代码质量与命令

提交前至少运行与当前改动相关的检查：

```sh
cargo fmt --all -- --check
cargo test --all-targets
cargo clippy --all-targets --all-features -- -D warnings
```

如果项目使用 SQLx 查询宏，补充数据库连接可用性和离线检查；如果增加前端或 E2E 工具，使用锁文件安装依赖并运行 Playwright 测试。命令以仓库实际 `Cargo.toml`、`package.json` 和 CI 配置为准，不要在文档中编造尚未存在的脚本。

Rust 代码应：

- 使用清晰的领域名和错误类型，不用无意义的 `unwrap` 掩盖可恢复错误。
- 在边界处解析和校验输入，在领域层保持不变量。
- 不在错误、日志、测试快照或调试输出中泄露令牌、个人数据和完整数据库 URL。
- 为并发共享状态明确选择所有权、锁或数据库事务边界。
- 新增依赖前确认它解决了当前需求，并评估 WASM、Linux、Docker 和许可证兼容性。

## 完成定义

一个功能只有同时满足以下条件才能视为完成：

- 对应的用户故事和验收标准已经实现，或文档明确记录了变更。
- 完成 Red-Green-Refactor；必要的错误、权限和边界场景有测试。
- 数据库变更有迁移，且在干净数据库上可以执行。
- API 由服务端执行输入校验、业务规则和评分。
- UI 具备加载、空状态和错误状态，并完成桌面、移动端浏览器验证。
- 相关 Rust 检查和 Playwright 流程通过。
- 配置、脚本和 Docker 行为不依赖未记录的本机状态。
